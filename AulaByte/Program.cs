using System;

namespace AulaByte
{
    class Program
    {
        static void Main(string[] args)
        {
            // byte (8-byte) -> 0 até 255
            // sbyte (8-byte) -> -128 até 127
            byte meuByte = 127;
            sbyte meuByteNegativo = -128;
        }
    }
}
